<article id="single">
    <head class="text-center">
        <h1 class="single_post_title" title="<%= @page.title %>">• <%= @page.title %></h1><hr> 
        <div class="row single_post_icons">
            <div class="col-xs-12 col-sm-4 text-center"><em class="glyphicon glyphicon-calendar"></em> <%= @page.created_at.strftime("%d %m %Y %H:%M") %></div>

        </div>
    </head>
    
    <hr>
    <section class="single_post_content"><%= image_tag @page.image.url, class:'single_post_cover', alt:@page.title  if @page.image.present? %> <%= @page.content.html_safe %></section>
</article>